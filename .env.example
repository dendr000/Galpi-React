# 파일 위치: Galpi/Galpi-React/.env
# 복사해서 .env로 저장 후 값을 채우세요: cp .env.example .env

# 백엔드(Galpi-Backend) 주소. 로컬 개발 시 보통 http://localhost:8080
VITE_API_BASE_URL=http://localhost:8080

# 위험한 삭제(전체 삭제 등) 실행 전 사용자가 직접 입력해서 확인하는 문구.
# 원하는 문자열로 자유롭게 지정하면 됩니다.
VITE_DELETE_KEYWORD=삭제확인
